---
phase: 03-legal-authorities-rag-panel
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - backend/requirements.txt
  - backend/models.py
  - backend/services/authorities.py
  - backend/services/authority_summary.py
  - backend/main.py
  - tests/test_authorities.py
autonomous: true
must_haves:
  truths:
    - "Authority lookup returns top-k records from the local corpus, not token-overlap hardcoded records."
    - "Authority relevance percent is derived from local cosine similarity."
    - "Each authority summary includes a deterministic verified/unverified result against the authority source text."
  artifacts:
    - path: "backend/services/authorities.py"
      provides: "Local vector retrieval and API response shaping"
    - path: "backend/services/authority_summary.py"
      provides: "Authority summary generation and exact quote verification"
    - path: "backend/main.py"
      provides: "/authorities endpoint using retrieval plus verified summaries"
    - path: "tests/test_authorities.py"
      provides: "Retrieval, relevance, endpoint, and verification tests"
  key_links:
    - from: "backend/services/authorities.py"
      to: "backend/data/authorities/corpus.jsonl"
      via: "loads persisted corpus records"
    - from: "backend/services/authorities.py"
      to: "sklearn.metrics.pairwise.cosine_similarity"
      via: "query-to-corpus scoring"
    - from: "backend/main.py"
      to: "backend/services/authority_summary.py"
      via: "summarizes retrieved chunks before response"
---

<objective>
Replace stub authority ranking with local vector retrieval and verified authority summaries.

Purpose: Satisfy RAG-02, RAG-03, and RAG-04 while preserving the project's core deterministic verification rule.
Output: Retrieval service, summary verifier, `/authorities` response fields, and backend tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-legal-authorities-rag-panel/03-CONTEXT.md
@.planning/phases/03-legal-authorities-rag-panel/03-RESEARCH.md
@.planning/phases/03-legal-authorities-rag-panel/03-01-SUMMARY.md
@backend/services/authorities.py
@backend/services/llm_finding.py
@backend/services/verifier.py
@backend/main.py
@tests/test_authorities.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build local cosine retrieval over the persisted corpus</name>
  <files>backend/requirements.txt, backend/services/authorities.py, tests/test_authorities.py</files>
  <action>Replace `_CORPUS` and token-overlap relevance with a file-backed corpus loader plus scikit-learn `TfidfVectorizer` and `cosine_similarity`. Add `scikit-learn` to backend/requirements.txt. Query text should combine the active finding text/quote when provided, falling back to document text/profile context for compatibility. Return top-k records sorted by raw cosine score descending. Map relevance to an integer percent only from the cosine score.</action>
  <verify>Run `uv run pytest tests/test_authorities.py` and confirm tests assert relevance comes from cosine scoring, not LLM output or token overlap.</verify>
  <done>Top-k authority ranking changes when the query changes, raw cosine scores drive relevance, and the endpoint still returns records for stored documents.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and verify authority summaries</name>
  <files>backend/models.py, backend/services/authority_summary.py, backend/services/authorities.py, tests/test_authorities.py</files>
  <action>Create an authority summary path that produces `{summary, quote}` per retrieved authority. Use the existing Anthropic/LLM pattern when available, but keep deterministic fallback summaries from `summary_seed` for tests and offline runs. Add verification fields to the authority response: `verified`, `verification_status` or equivalent, and quote span fields within source_text if useful. Verification must be exact `source_text.find(quote)`, with no fuzzy matching and no LLM self-attestation.</action>
  <verify>Run `uv run pytest tests/test_authorities.py tests/test_verifier.py`; include tests for verified true when quote is exact and verified false when quote is absent while still returning the authority.</verify>
  <done>Each authority entry has summary, quote, verified flag, relevance, source type, title, and URL; unverified authority summaries remain visible in the response.</done>
</task>

<task type="auto">
  <name>Task 3: Extend `/authorities` request/response for finding-triggered lookup</name>
  <files>backend/main.py, backend/models.py, tests/test_authorities.py</files>
  <action>Extend `AuthoritiesRequest` to accept optional `finding`, `quote`, and `source_chunk_id` fields while keeping `doc_id` and `profile_id` backward-compatible. Build the retrieval query from the finding-specific fields when supplied. Ensure response records are tab-ready and include all UI-needed fields: authority_id, title, source_type, summary, relevance, quote, verified, url, and optional raw_score/debug metadata only if useful for tests.</action>
  <verify>Run `uv run pytest tests/test_authorities.py`; include endpoint tests for both document-level lookup and finding-specific lookup.</verify>
  <done>Frontend can trigger authority lookup for a specific finding without breaking existing document-level analyze behavior.</done>
</task>

</tasks>

<verification>
This plan is complete when authority lookup is corpus-backed, cosine-scored, and quote-verified.
</verification>

<success_criteria>
- RAG-02 top-k retrieval is local and deterministic.
- RAG-03 summaries use exact quote verification against authority source text.
- RAG-04 relevance percent is derived from cosine score.
</success_criteria>

<output>
After completion, create `.planning/phases/03-legal-authorities-rag-panel/03-02-SUMMARY.md`.
</output>
