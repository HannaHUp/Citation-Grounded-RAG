---
phase: 03-legal-authorities-rag-panel
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - frontend/src/types.ts
  - frontend/src/api.ts
  - frontend/src/App.tsx
  - frontend/src/components/FindingCard.tsx
  - frontend/src/components/FindingsPanel.tsx
  - frontend/src/components/LegalAuthoritiesPanel.tsx
  - frontend/src/styles.css
autonomous: false
must_haves:
  truths:
    - "User can trigger authority lookup for a finding."
    - "Legal Authorities panel separates Cases, Statutes & Regulations, and Secondary sources."
    - "Each authority card shows title, relevance percent, verified/unverified badge, summary, verbatim quote block, and real source link."
    - "Loading, empty, and error states explain what is happening."
  artifacts:
    - path: "frontend/src/components/LegalAuthoritiesPanel.tsx"
      provides: "Tabbed authority panel with verified summary cards"
    - path: "frontend/src/api.ts"
      provides: "Finding-specific authorities API call"
    - path: "frontend/src/types.ts"
      provides: "Authority response fields including verified"
    - path: "frontend/src/App.tsx"
      provides: "State and handlers for finding-triggered lookup"
  key_links:
    - from: "frontend/src/components/FindingCard.tsx"
      to: "frontend/src/App.tsx"
      via: "authority lookup callback"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/api.ts"
      via: "getAuthorities with finding fields"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/LegalAuthoritiesPanel.tsx"
      via: "authority state props"
---

<objective>
Wire the Legal Authorities panel into the existing React UI as Surface B.

Purpose: Satisfy RAG-05 without pulling in the Phase 4 workflow shell.
Output: Finding-triggered lookup controls, tabbed authority cards, verified badge display, and practical UI states.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-legal-authorities-rag-panel/03-CONTEXT.md
@.planning/phases/03-legal-authorities-rag-panel/03-RESEARCH.md
@.planning/phases/03-legal-authorities-rag-panel/03-02-SUMMARY.md
@frontend/src/App.tsx
@frontend/src/api.ts
@frontend/src/types.ts
@frontend/src/components/FindingsPanel.tsx
@frontend/src/components/FindingCard.tsx
@frontend/src/components/LegalAuthoritiesPanel.tsx
@frontend/src/components/VerifiedBadge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add finding-triggered authority lookup state and API call</name>
  <files>frontend/src/types.ts, frontend/src/api.ts, frontend/src/App.tsx, frontend/src/components/FindingCard.tsx, frontend/src/components/FindingsPanel.tsx</files>
  <action>Extend `LegalAuthority` type with backend fields including `verified`. Update `getAuthorities` to accept optional finding data. Add UI control from each finding card to look up authorities for that finding; pass finding, quote, and source_chunk_id to the API. Keep document-level authority lookup for the antitrust profile if still present, but finding-triggered lookup should be the primary Phase 3 behavior.</action>
  <verify>Run `cd frontend; npm run build` and manually inspect TypeScript errors. Run backend tests if API type changes require alignment.</verify>
  <done>User can request authorities for a specific finding and the request includes finding-specific context.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade authority panel cards and states</name>
  <files>frontend/src/components/LegalAuthoritiesPanel.tsx, frontend/src/components/VerifiedBadge.tsx, frontend/src/styles.css</files>
  <action>Render tabs for Cases, Statutes & Regulations, and Secondary sources. Remove the optional All tab if it weakens the required tab separation. Each card must show source type, relevance percent, title, verified/unverified badge, AI summary, verbatim quote block, and source link. Add states for loading, retrieval error, no corpus/ingestion missing, and no matches. Keep cards compact and consistent with existing findings UI; do not introduce the Phase 4 landing/workflow shell.</action>
  <verify>Run `cd frontend; npm run build`; visually verify that long titles, quotes, and empty tab content do not overlap at desktop and mobile widths.</verify>
  <done>Authority panel communicates relevance and verification clearly, with usable tabs and no hidden quote support.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify the Legal Authorities panel</name>
  <files>frontend/src/App.tsx, frontend/src/components/LegalAuthoritiesPanel.tsx, frontend/src/styles.css</files>
  <action>Pause after automated frontend work so the user can verify the end-to-end Surface B behavior in a browser.</action>
  <what-built>Surface B Legal Authorities panel wired to finding-triggered lookup.</what-built>
  <how-to-verify>
    1. Start backend and frontend.
    2. Upload the complaint fixture or a contract/complaint document.
    3. Run document analysis to produce findings.
    4. Click authority lookup on a finding.
    5. Confirm authorities load into Cases, Statutes & Regulations, and Secondary tabs.
    6. Confirm each card has relevance percent, verified/unverified badge, summary, quote block, and real source link.
  </how-to-verify>
  <verify>User follows the browser verification steps and confirms the authority panel behavior.</verify>
  <done>User approves the Surface B behavior or reports issues for follow-up.</done>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<verification>
This plan is complete when RAG-05 is visible and usable in the existing app.
</verification>

<success_criteria>
- Authority lookup can be triggered from a finding.
- Tabs separate the required source types.
- Authority cards show summary, relevance, quote, verification, and URL.
- Empty/loading/error states are clear.
</success_criteria>

<output>
After completion, create `.planning/phases/03-legal-authorities-rag-panel/03-03-SUMMARY.md`.
</output>
