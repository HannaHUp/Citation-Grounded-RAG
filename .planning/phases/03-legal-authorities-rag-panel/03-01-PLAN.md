---
phase: 03-legal-authorities-rag-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/models.py
  - backend/services/authority_ingest.py
  - backend/scripts/ingest_authorities.py
  - backend/data/authorities/corpus.jsonl
  - backend/data/authorities/ingest_state.json
  - tests/test_authority_ingest.py
autonomous: true
must_haves:
  truths:
    - "System can ingest a small real legal corpus with cases, statutes/regulations, and secondary sources."
    - "Interrupted ingestion can resume without duplicating already-written authorities."
    - "Every persisted authority chunk keeps verbatim source text and a real source URL."
  artifacts:
    - path: "backend/services/authority_ingest.py"
      provides: "Checkpointed authority corpus ingestion"
    - path: "backend/scripts/ingest_authorities.py"
      provides: "CLI entry point for ingestion"
    - path: "backend/data/authorities/corpus.jsonl"
      provides: "Inspectable local authority corpus"
    - path: "backend/data/authorities/ingest_state.json"
      provides: "Resume checkpoint state"
  key_links:
    - from: "backend/scripts/ingest_authorities.py"
      to: "backend/services/authority_ingest.py"
      via: "imports and calls ingestion service"
    - from: "backend/services/authority_ingest.py"
      to: "backend/data/authorities/corpus.jsonl"
      via: "append normalized authority records"
    - from: "backend/services/authority_ingest.py"
      to: "backend/data/authorities/ingest_state.json"
      via: "read/write checkpoint cursor"
---

<objective>
Build the real local legal corpus foundation for Phase 3.

Purpose: Replace the hardcoded demo-only corpus with an inspectable, resumable corpus ingestion path that satisfies RAG-01 and supports all required authority tabs.
Output: Checkpointed ingestion service, CLI script, seed corpus files, and backend tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-legal-authorities-rag-panel/03-CONTEXT.md
@.planning/phases/03-legal-authorities-rag-panel/03-RESEARCH.md
@backend/services/authorities.py
@tests/test_authorities.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define persisted authority corpus model and storage paths</name>
  <files>backend/models.py, backend/services/authority_ingest.py, backend/data/authorities/corpus.jsonl, backend/data/authorities/ingest_state.json</files>
  <action>Create dataclasses or typed dictionaries for persisted authority records with fields: authority_id, title, source_type ("case" | "statute" | "secondary"), summary_seed, quote_seed, url, source_text, court/source metadata, and source_hash. Add storage constants under backend/data/authorities. Seed the corpus with a small set of real statute/regulation and secondary records using real URLs so Stat. & Reg. and Secondary tabs are never empty. Preserve verbatim source_text exactly as quotes will be verified against.</action>
  <verify>Run `uv run pytest tests/test_authority_ingest.py tests/test_authorities.py` after adding tests in Task 3.</verify>
  <done>Corpus files exist, records include all three source types, every seed record has quote_seed contained in source_text, and records have real URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement checkpointed CourtListener ingestion</name>
  <files>backend/services/authority_ingest.py, backend/scripts/ingest_authorities.py, backend/requirements.txt</files>
  <action>Add a CourtListener ingestion function that fetches a bounded number of case records from `https://www.courtlistener.com/api/rest/v4/search/`, normalizes each result into the local authority record shape, appends only new authority IDs, and writes `ingest_state.json` with next URL/cursor, completed flag, and count. Use simple HTTP code and timeouts; if adding `requests`, pin it in backend/requirements.txt. The CLI should support `--limit` and `--resume` and call the service without requiring the FastAPI server.</action>
  <verify>Run `uv run pytest tests/test_authority_ingest.py`; include a mocked HTTP response test proving next cursor/state is persisted and duplicate authority IDs are skipped on resume.</verify>
  <done>Interrupted ingestion can be resumed and duplicate records are not written twice.</done>
</task>

<task type="auto">
  <name>Task 3: Add ingestion tests for real-source invariants</name>
  <files>tests/test_authority_ingest.py</files>
  <action>Create tests for: seeded records cover case/statute/secondary types; persisted records have source_text, quote_seed, url, and title; quote_seed is exactly found in source_text; checkpoint state is read and written; repeated ingestion with the same authority ID remains idempotent.</action>
  <verify>Run `uv run pytest tests/test_authority_ingest.py tests/test_authorities.py`.</verify>
  <done>Tests fail if source text is normalized away, if source tabs are missing, or if resume duplicates records.</done>
</task>

</tasks>

<verification>
This plan is complete when RAG-01 is backed by a real local corpus path, not hardcoded in-memory authorities only.
</verification>

<success_criteria>
- `backend/data/authorities/corpus.jsonl` is inspectable and covers all source types.
- CourtListener ingestion has checkpoint/resume behavior.
- Tests prove idempotency and exact quote containment.
</success_criteria>

<output>
After completion, create `.planning/phases/03-legal-authorities-rag-panel/03-01-SUMMARY.md`.
</output>
